 - Update software list 	>sudo apt-get update
 - Download dependencies	>sudo apt-get install patch build-essential libreadline-dev libncurses-dev libusb-dev libftdi-dev automake autoconf bison flex
 - Build the dependecies	>sudo apt-get build-dep avrdude avrdude-doc
 - Build libusb manually	>cd <libusb>
				>./congifure		(permission denied use chmod +x ./configure to get sudo)
				>make
				>sudo make install

 - Build libusb-compat manually	>cd <libusb-compat>
				>./congifure		(permission denied use chmod +x ./configure to get sudo)
				>make
				>sudo make install

 - Build libftdi manually	>cd <libftdi>
				>./congifure		(permission denied use chmod +x ./configure to get sudo)
				>make
				>sudo make install

 - Build AVRDUDEsvn-patched	>cd <AVRDUDE>
				>./bootstrap
				>./configure
				>./make
				>./sudo make install

You may need to set access permissions as root to a port, replace ??? with the port to be used:

 - sudo chmod a+rw /dev/????